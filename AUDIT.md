# AIVOA — Technical Audit & Gap Analysis Report

| ID | Issue Description | Severity | File(s) | Required Fix |
|---|---|---|---|---|
| **SEC-01** | Hardcoded credentials in `.env.example` and `backend/app/db/session.py` fallback | High | `.env.example`, `backend/app/db/session.py` | Sanitize `.env.example` with standard placeholders (`user:password@localhost:5432/dbname`). Remove credentials fallback in `session.py` and require clean env variable. |
| **SEC-02** | Stale API key prefix and credentials in documentation | Medium | `README.md` | Remove `gsk_` prefix and credentials in README examples. Use sanitized placeholders only. |
| **SEC-03** | Exception stack traces and internal DB errors leaked in API responses | Medium | `backend/app/api/routes_ai.py`, `backend/app/api/routes_complaints.py`, `backend/app/utils/file_parser.py` | Replace raw `str(e)` in `HTTPException(detail=...)` with controlled, user-safe error messages and log internal details on the server. |
| **PORT-01** | Stale port 8001 hardcoded in backend server runner, Axios client, test suite, and docs | High | `backend/main.py`, `frontend/src/api/apiClient.js`, `test_e2e_workflow.py`, `README.md` | Standardize port to `8000` for backend across all configuration, code, tests, and documentation. |
| **AI-01** | `gemma2-9b-it` decommissioned by Groq; unauthorized silent replacement with prohibited `qwen/qwen3.8-27b` | High | `backend/app/ai/groq_client.py`, `README.md` | Remove `qwen/qwen3.8-27b`. Implement verified Groq model fallback (`openai/gpt-oss-120b`), emit explicit `logger.warning()`, allow override via `GROQ_MODEL`, and document reason in README. |
| **AI-02** | Missing Pydantic validation on LLM JSON output in LangGraph nodes | High | `backend/app/ai/nodes/extract_fields.py`, `classify_severity.py`, `risk_assessment.py` | Enforce full validation pipeline: LLM -> JSON parsing -> Pydantic schema validation (`ExtractedComplaintFields`, `SeverityClassificationResult`, `RiskAssessmentResult`) with safe default fallbacks. |
| **AI-03** | Provider failures converted into successful Copilot chat strings | High | `backend/app/ai/graph.py` | Stop returning error strings as successful assistant replies in `chat_with_copilot`. Raise appropriate exceptions to trigger controlled 401, 429, or 5xx HTTP responses. |
| **COP-01** | Copilot blocked from text-only chat without uploaded document | High | `backend/app/ai/graph.py`, `frontend/src/components/AICopilot/CopilotChat.jsx` | Allow Copilot conversation without document upload. Initialize general pharma QMS conversational state when no document is attached. Remove disabled input restriction. |
| **COP-02** | Copilot composer missing attachment integration and document-assisted context switch | Medium | `frontend/src/components/AICopilot/CopilotChat.jsx`, `backend/app/api/routes_ai.py` | Add document attachment button directly to Copilot composer. When a document is attached to an existing session, preserve `thread_id`, discard unrelated prior chat from complaint context, and start clean complaint analysis. |
| **SES-01** | `New Intake` does not mint a new session `thread_id` and leaves thread in null state | Medium | `frontend/src/store/slices/aiCopilotSlice.js`, `frontend/src/components/Navbar.jsx` | Ensure `New Intake` creates a fresh UUID `thread_id`, clears complaint state, AI state, uploaded file, chat, and error state cleanly. |
| **PROV-01** | User-edited form fields overwritten on subsequent AI extractions / Redux updates; casing inconsistency | Medium | `frontend/src/store/slices/complaintSlice.js` | Preserve `USER` provenance: if a field has been edited by the user (`fieldMetadata[field] === 'USER'`), do not overwrite it on subsequent AI populates. Standardize provenance values to `AI`, `USER`, `MISSING`. |
| **VAL-01** | Frontend dropzone fails to validate empty files (0 bytes) before uploading | Low | `frontend/src/components/AICopilot/FileDropzone.jsx` | Add explicit client-side check rejecting 0-byte files with the same controlled error message as the backend. |
| **DB-01** | Unhandled duplicate complaint ID and database unavailability | Medium | `backend/app/api/routes_complaints.py` | Catch `IntegrityError` specifically and return HTTP 409 Conflict with clean message; ensure rollback on all failures and controlled 503 on database unavailability. |
| **DOC-01** | README and `.env.example` out of sync with actual architecture, models, and ports | Medium | `README.md`, `.env.example` | Update README with accurate setup steps, port 8000, model migration details, test commands, and complete API endpoint documentation. |
| **CLN-01** | Machine-specific bundled virtualenv `alvoa/` and build artifacts in repository | Medium | Project root (`alvoa/`, `frontend/dist/`) | Clean up machine-specific virtual environment and build folders. |
